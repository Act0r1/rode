Rode - it is Linux native alternative for T3Code, Codex App. They work pretty well but only in Mac(maybe Windows). For now suppport only auth for OpenAI gpt models.

Every new feature that i ask realize - create new worktree. For all simple tasks send simple agents. Send Terra or Luna, be the orchestrator, not the executor, go do it yourself, only if needed Send Terra or Luna, be the orchestrator, not the executor, go do it yourself, only if needed.
After finishing tasks, go merge it into master. If encounter any error ask me. After delete worktree
